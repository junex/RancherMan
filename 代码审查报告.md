# Rancher助手 代码审查报告

> 审查日期：2026-04-29
> 项目：RancherMan — Rancher 集群管理桌面 GUI 工具
> 技术栈：Go 1.22 + Fyne v2.5.2 + SQLite/GORM + SSH

---

## 一、架构与设计

### 1.1 全局变量泛滥（最严重） ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-04-30）</b></summary>

**修复方案：** 方案B — AppState + RWMutex + fyne.Do

**改动文件：**
- `operations/state.go`（新建）— AppState 结构体封装全部 23 个字段，`sync.RWMutex` 保护
- `operations/globals.go` — 删除包级 var 块，全部 Getter/Setter 加读写锁
- `main.go` — `taskQueueUI` 回调用 `fyne.Do`/`fyne.DoAndWait` 调度到主线程
- `operations/info_display.go` — `gXxx` 直接引用 → `GetXxx()` 加锁访问
- `operations/workload_ops.go` — 同上

**验证：** `go build ./...` / `go vet ./...` / `go build -race .` 全部通过

</details>

> 详细 goroutine 模型、调用链追踪、每种竞争场景分析及修复方案见 **[第十章 专题：全局变量并发问题深入分析](#十专题全局变量并发问题深入分析)**。

### 1.2 分层架构破裂 ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-04-30）</b></summary>

**修复方案：** operations 层定义接口 + ui 层提供适配器

**改动文件：**
- `operations/interfaces.go`（新建）— 4 个接口：`InfoDisplayer`、`ListRefresher`、`TextGetter`、`UIDispatcher`
- `ui/adapters.go`（新建）— `entryAdapter` 包装 `*widget.Entry`、`fyneDispatcher` 包装 `fyne.Do`
- `operations/state.go` — 删除 `fyne`/`widget`/`component` 导入；UI 字段改为接口类型；删除 5 个死字段（`App`、`TaskStatusBar`、`OperationButtons`、`WorkloadList`、`WorkloadSearch`）
- `operations/globals.go` — 删除全部 Fyne/component 导入；删除 6 对无用 Getter/Setter；接口化剩余 Setter/Getter
- `ui/main_window.go` — `InitView` 返回 `InitResult` 结构体；通过适配器注入 UI 组件
- `ui/globals.go` — 移除 `gTaskStatusBar` 和 `gOperationButtons`
- `main.go` — 从 `InitResult` 获取 UI 组件；`taskQueueUI` 直接持有引用

**验证：** `go build ./...` / `go vet ./...` 全部通过。operations 包零 Fyne import。

</details>

> **已知问题：** 存在严重 bug，待修复。

### 1.3 UI 层越级调用基础设施层 ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-05-06）</b></summary>

**修复方案：** operations 层新增 5 个包装函数，UI 层不再直接调用 rancher 包函数。

**改动文件：**
- `operations/globals.go` — 新增 `GetEnvironmentFromConfig`、`SaveConfig`、`GetConfigContent`、`GetWorkloadsByNamespace`、`GetAllNamespacesDetail`
- `ui/main_window.go` — 6 处 `rancher.*` / `db.*` 调用改为 `operations.*`；`ShowSelectNamespaceDialog` 调用去掉 `db` 参数
- `ui/CustomDiaolog.go` — 去掉 `db *rancher.DatabaseManager` 参数，改用 `operations.GetAllNamespacesDetail()`

**验证：** `go build ./...` / `go vet ./...` 全部通过。

</details>

> 原问题：`ui/main_window.go` 直接调用 `rancher.GetEnvironmentFromConfig`、`rancher.SaveConfigToDb`、`db.GetWorkloadsByNamespace` 等，跳过了 `operations` 业务层。职责边界模糊，同样的逻辑分散在 UI 和 operations 两处。

---

## 二、安全隐患

### 2.1 TLS 证书验证完全跳过 🔒 不处理

```go
// rancher/RancherApi.go:69
TLSClientConfig: &tls.Config{InsecureSkipVerify: true}
```

```go
// rancher/SSHUtils.go:47
HostKeyCallback: ssh.InsecureIgnoreHostKey()
```

所有 HTTPS 和 SSH 连接都无法防御中间人攻击。

**建议：** 至少提供配置项允许用户选择是否跳过验证，默认开启验证。

### 2.2 敏感信息明文暴露 🔒 不处理

| 位置 | 敏感信息 |
|------|---------|
| [RancherUtils.go:245](rancher/RancherUtils.go#L245) | 跳板机密码明文存入 SQLite |
| [info_display.go:88-95](operations/info_display.go#L88-L95) | MySQL/MongoDB root 密码直接显示在 GUI |
| config 表 | API 密钥明文存储 |

**建议：** 密码字段使用系统 keystore 或 aes 加密存储；数据库密码在 UI 上默认掩码显示。

### 2.3 SSH 命令拼接 🔒 不处理

```go
// SSHUtils.go:94
cmd := fmt.Sprintf("/bin/bash -c 'cd %s && find . -type f ...'", jumpHostConfig.RootPath)
```

`RootPath` 来自用户配置，如果被篡改可能注入恶意命令。

**建议：** 对路径参数做白名单校验。

---

## 三、并发与正确性

### 3.1 任务队列死锁风险 ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-05-06）</b></summary>

**修复方案：** 将 `tq.taskChan <- task` 移到 `tq.mu.Unlock()` 之后，channel 发送不再持锁。

**改动文件：**
- `rancher/taskqueue.go` — `AddTask` 中 `defer Unlock()` 改为显式 `Unlock()`，channel 发送移到锁外

**风险分析：** CancelAll 可能在 Unlock 和 channel send 之间执行，导致已取消的任务仍被发送。执行器接收后检查 `cancelRequested` 会跳过该任务，结果无害。任务间无顺序依赖，channel 发送顺序变化无影响。

</details>

> 原问题：`AddTask` 在持有 mutex 的情况下同步向 channel 发送，channel 容量 100 满了就永久阻塞，所有需要 `tq.mu` 的操作（包括 `CancelAll`）全部卡死——**死锁**。

### 3.2 HTTP Response Body 泄漏

多个 API 函数在 JSON 解码失败时未关闭 response body：

```go
// RancherApi.go:151-153（同样问题在 GetNamespaceList, GetPodList, GetServiceList, GetConfigMapList）
if err := json.NewDecoder(resp.Body).Decode(&workloadsResponse); err != nil {
    return nil, err  // ← resp.Body 未关闭
}
```

**建议：** 立即资源释放 — 先 `defer resp.Body.Close()` 再进行 `json.Decode`。

### 3.3 SSH Session 泄漏

[SSHUtils.go:86-91](rancher/SSHUtils.go#L86-L91) 第一个 session 用 `defer session.Close()` 注册，但循环内（第176行）又用 `session, err = client.NewSession()` **覆盖**了变量名。`defer` 最终关闭的是循环中最后创建的那个 session，第一个 session 泄漏。

```go
session, err := client.NewSession()  // session A
defer session.Close()                 // 注册关闭 session（但变量会被覆盖）

// ... 循环内 ...
session, err = client.NewSession()    // session B — 覆盖了变量
// defer 此时指向 session B，session A 永远不会被 Close
```

---

## 四、数据一致性与可靠性

### 4.1 全量更新无事务回滚

[UpdateEnvironment](rancher/RancherUtils.go#L92-L173) 采用"先删后插"策略：

```go
db.DeleteNamespaceByEnvironment(environment.ID)   // 已删除！
db.InsertNamespaces(namespaceDBList)              // 成功
// ...
db.DeleteWorkloadByEnv(environment.ID)            // 已删除！
workloadList, err := GetWorkloadList(...)          // 如果这里失败了——
db.InsertWorkloads(workloadsDBList)               // 永不被执行
```

旧数据已删，新数据没插进去，数据丢失且无回滚。

**建议：** 先拉取新数据到内存，成功后再在一个事务中删旧插新；或使用临时表方案。

### 4.2 SaveConfig 无错误处理

```go
// RancherUtils.go:87-89
func SaveConfigToDb(db *DatabaseManager, content string) {
    db.DeleteConfig(1)       // 忽略错误
    db.InsertConfig(1, content)  // 忽略错误
}
```

删除成功 + 插入失败 → 配置永久丢失。

### 4.3 错误被大量忽略

```go
// main_window.go:407
environment, _ := rancher.GetEnvironmentFromConfig(...)

// workload_ops.go:88
destEnvironment, _ := rancher.GetEnvironmentFromConfig(...)

// RancherUtils.go:130
configList, _ := ParseNginxConfig(...)

// RancherApi.go:93
jsonPayload, _ := json.Marshal(payload)
```

关键操作一旦出错 UI 上无任何反馈，用户无法感知失败。

---

## 五、性能

### 5.1 每次 HTTP 请求创建新 Client ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-05-06）</b></summary>

**修复方案：** `sync.Once` 惰性初始化包级共享 `http.Client`，所有请求复用同一个 Transport 连接池。

**改动文件：**
- `rancher/RancherApi.go` — 新增 `httpClient` + `httpClientOnce` + `getHTTPClient()`，`makeRequest` 中的 `new(http.Client)` 改为 `getHTTPClient().Do(req)`

</details>

> 原问题：每调用一次就 `new(http.Client)`，导致无法复用 TCP 连接，每次请求都要重新三次握手 + TLS 握手，产生大量 TIME_WAIT 连接。

### 5.2 逐条 INSERT 性能差 ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-05-06）</b></summary>

**修复方案：** 5 个 Insert 函数的事务内 `for` 循环 `tx.Create` 改为 `tx.CreateInBatches(records, 100)`，删除未使用的 `BatchCreateWorkloads`。

**改动文件：**
- `rancher/RancherDatabase.go` — `InsertWorkloads`、`InsertNamespaces`、`InsertPods`、`InsertUploadConfigs`、`InsertServices` 全部统一批量插入

</details>

> 原问题：事务中逐条 Create，每条一个 SQL，批量数据慢 10~100 倍。

### 5.3 SQLite 无性能调优 ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-05-06）</b></summary>

**修复方案：** `NewDatabaseManager` 中 `gorm.Open` 后设置 4 个 pragma。

**Pragma 清单：**
- `journal_mode=WAL` — 读写不互斥
- `synchronous=NORMAL` — WAL 下安全，减少 fsync
- `cache_size=-8000` — 8MB 页面缓存
- `busy_timeout=5000` — 锁等待 5 秒而非立即报错

**改动文件：**
- `rancher/RancherDatabase.go` — `NewDatabaseManager` 新增 pragma 设置

</details>

> 原问题：未开启 WAL 导致读写互斥，未调优缓存和同步模式。

---

## 六、代码质量

### 6.1 Debug 代码残留

```go
// workload_ops.go:383-385
for i := range workloads {
    fmt.Printf("已处理 %s\n", workloads[i].Name)  // 应使用 log.Printf 或删除
}
```

### 6.2 空错误处理块

```go
// SSHUtils.go:184-186
if err != nil {
} else {
}
```

### 6.3 硬编码散落各处

| 位置 | 硬编码内容 |
|------|-----------|
| [workload_ops.go:388-398](operations/workload_ops.go#L388-L398) | 工作负载优先级规则 |
| [RancherApi.go:237](rancher/RancherApi.go#L237) | `defaultNamespace: "big-data"` |
| [main_window.go:383](ui/main_window.go#L383) | 窗口默认大小 1050x600 |
| [SSHUtils.go:108](rancher/SSHUtils.go#L108) | PTY 终端大小 80x40 |

### 6.4 RefreshList 只刷新前 10 项 ✅ 已修复

```go
// ui/component/Widgets.go:113-115
func (t *MultiSelectList) RefreshList() {
    t.ScrollToTop()
    t.list.Refresh()
}
```

列表超过 10 项时，第 11 项及以后的选中状态在滚动后可能显示不一致。

> **修复**：移除硬编码的 `i < 10` 限制，改为 `t.list.Refresh()` 让 Fyne 自行决定需要刷新的可见项。同时 `RefreshAllItems` 也改为 `t.list.Refresh()` 替代无效的 `t.BaseWidget.Refresh()`。

### 6.5 Nginx 解析器无法处理嵌套 location ✅ 已修复

[Parser.go](rancher/Parser.go) 正则 `[^}]+` 只匹配第一个 `}`，嵌套 `location` 或内含 `if {}` 等带花括号的块会导致解析错误。

> **修复**：用括号计数（深度计数器）替代 `[^}]+` 正则，精确匹配 `{` 对应的 `}`。同时 `searchStart` 从 `{` 后继续搜索，确保嵌套 location 都能被找到。

---

## 七、数据库设计

### 7.1 Config 表实质是单行表

Config 表只有一条记录（ID=1），字段是 YAML 文本 blob。可改用文件存储或结构化字段。

### 7.2 字段长度限制过严

```go
Workload.Name  string `gorm:"size:30"`   // 部分 K8s 工作负载名可能超30字符
Workload.Image string `gorm:"size:100"`  // 镜像名+tag 可能超100字符
```

### 7.3 缺少索引 ✅ 已修复

<details>
<summary><b>修复状态：已完成（2026-05-06）</b></summary>

**修复方案：** 在 GORM 模型 struct tag 中为高频查询字段添加 `index`。

**索引清单：**
- Workload: `environment`、`namespace`
- Namespace: `environment`
- Pod: `environment`、`namespace_id`、`workload_id`
- Service: `environment`

**改动文件：**
- `rancher/RancherDatabase.go` — 4 个模型新增 index tag，GORM AutoMigrate 自动创建

</details>

> 原问题：高频查询字段未建索引，数据量大后查询变慢。

---

## 八、问题优先级汇总

| 优先级 | 类别 | 问题 | 影响 | 状态 |
|--------|------|------|------|------|
| **P0** | 并发 | 全局变量无锁访问 | panic / 数据竞争 | ✅ 已修复 |
| **P0** | 并发 | 任务队列持锁阻塞发送 | 死锁 | ✅ 已修复 |
| **P0** | 资源 | HTTP body 泄漏 | 内存/连接泄漏 | |
| **P1** | 安全 | TLS/SSH 跳过证书验证 | 中间人攻击 | 🔒 不处理 |
| **P1** | 安全 | 密码明文存储和显示 | 信息泄露 | 🔒 不处理 |
| **P1** | 安全 | SSH 命令拼接注入风险 | 命令注入 | 🔒 不处理 |
| **P1** | 数据 | 全量更新无事务回滚 | 数据丢失 | |
| **P1** | 数据 | SaveConfig 无错误处理 | 配置丢失 | |
| **P1** | 资源 | SSH session 泄漏 | 连接泄漏 | |
| **P2** | 架构 | 分层架构破裂（Fyne依赖） | 可维护性差 | ✅ 已修复 |
| **P2** | 架构 | UI 越级调用基础设施层 | 职责边界模糊 | ✅ 已修复 |
| **P2** | 性能 | 每请求创建 HTTP Client | 高延迟 | ✅ 已修复 |
| **P2** | 性能 | 逐条 INSERT | 数据同步慢 | ✅ 已修复 |
| **P2** | 性能 | SQLite 无性能调优 | 读写互斥 | ✅ 已修复 |
| **P3** | 数据库 | 缺少索引 | 查询变慢 | ✅ 已修复 |
| **P3** | 代码 | 错误忽略 / 硬编码 / debug 残留 | 可维护性 | |

---

## 九、重构路线图建议

```
阶段1（稳定修复）
├── 修复 HTTP body 泄漏（+defer）
├── 修复 SSH session 泄漏
├── 修复任务队列死锁（channel 发送移出锁）
├── 修复 SaveConfig 错误处理
└── 修复全量更新回滚逻辑（先插后删）

阶段2（安全加固）
├── TLS 证书验证可配置化
├── SSH host key 验证
├── 密码字段 AES 加密存储
└── UI 敏感信息掩码显示

阶段3（架构改进）
├── ✅ 引入 AppState + sync.RWMutex 替代全局变量（2026-04-30 已实施）
├── ✅ fyne.Do/DoAndWait 解决跨 goroutine UI 更新
├── ✅ operations 层去除 Fyne 依赖（2026-04-30 已实施，存在已知 bug 待修复）
├── 统一错误处理策略
└── HTTP Client 单例化

阶段4（性能与质量）
├── 统一批量 INSERT
├── SQLite pragma 优化
├── 数据库索引
├── 清理硬编码和 debug 代码
└── 补充单元测试
```

---

## 十、专题：全局变量并发问题深入分析

### 10.1 当前 Goroutine 模型

整个应用存在 **4 个 goroutine** 会访问全局变量：

```
┌─────────────────────────────────────────────────────────────────┐
│ Goroutine 1: MAIN (Fyne 事件循环)                                │
│ window.ShowAndRun()                                              │
│ ├─ 所有 UI 事件回调（按钮点击、列表选择、搜索输入）都在此执行       │
│ ├─ 菜单回调 → LoadConfig, UpdateDataTask ...                     │
│ ├─ 列表渲染 → 读 gFilteredNamespaces, gFilteredWorkloads         │
│ └─ 信息区更新 → UpdateInfoArea → 读 gSelectedWorkloads 等         │
├─────────────────────────────────────────────────────────────────┤
│ Goroutine 2: time.AfterFunc (main.go:40)                        │
│ 启动后 300ms 触发一次                                              │
│ ├─ 读 gConfig → 遍历 environment map                             │
│ └─ 调 UpdatePodsTask → taskQueue.AddTask                         │
├─────────────────────────────────────────────────────────────────┤
│ Goroutine 3: UI 状态刷新定时器 (taskqueue.go:242)                 │
│ 每 500ms 触发                                                      │
│ ├─ 读 tq.currentTask, tq.tasks (持锁)                            │
│ ├─ 调 taskQueueUI.UpdateStatus                                   │
│ │  └─ taskStatusBar.Update()   ← Fyne widget 操作!              │
│ │  └─ btn.Enable()/Disable()   ← Fyne widget 操作!              │
├─────────────────────────────────────────────────────────────────┤
│ Goroutine 4: 任务执行器 (taskqueue.go:276)                        │
│ 从 channel 消费任务，顺序执行                                      │
│ ├─ executeTask → Scale/Redeploy/UpdateEnvironment...             │
│ └─ OnTaskComplete (main.go:74)                                    │
│    ├─ operations.InitData()                                       │
│    │  ├─ 写 gNamespaces                                          │
│    │  ├─ 写 gFilteredNamespaces                                  │
│    │  └─ 写 gSelectedNamespace                                   │
│    └─ operations.UpdateInfoArea()                                 │
│       ├─ 读 gSelectedWorkloads, gSelectedNamespace, gEnvironment │
│       └─ gInfoArea.SetText()  ← Fyne widget 操作!               │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 全局变量分类与风险评估

将 23 个全局变量按风险分为三组：

#### A 组：一次性初始化，之后只读（低风险）

| 变量 | 写入时机 | 读取者 | 风险 |
|------|---------|--------|------|
| `gDb` | main.go SetDb（启动时） | 所有层 | 无竞态 |
| `gTaskQueue` | main.go（启动时） | 所有层 | 无竞态 |
| `gApp` | InitView（启动时） | ui 层 | 无竞态 |
| `gNamespaceList` | InitView（启动时） | ui 层、operations | 无竞态 |
| `gNamespaceSearch` | InitView（启动时） | ui 层、operations | 无竞态 |
| `gWorkloadList` | InitView（启动时） | ui 层、operations | 无竞态 |
| `gWorkloadSearch` | InitView（启动时） | ui 层、operations | 无竞态 |
| `gInfoArea` | InitView（启动时） | 所有层 | **⚠ 操作 Fyne widget，见 10.4** |
| `gTaskStatusBar` | InitView（启动时） | goroutine 3 | **⚠ 操作 Fyne widget，见 10.4** |
| `gOperationButtons` | InitView（启动时） | goroutine 3 | **⚠ 操作 Fyne widget，见 10.4** |

#### B 组：界面交互时写入，任务回调时读取（中风险）

| 变量 | 写 goroutine | 读 goroutine | 风险场景 |
|------|-------------|-------------|---------|
| `gSelectedWorkloads` | MAIN（多选回调） | G4（OnTaskComplete→UpdateInfoArea） | slice header 撕裂 |
| `gFilteredWorkloads` | MAIN（搜索、选择命名空间） | G4（OnTaskComplete→UpdateInfoArea） | slice header 撕裂 |
| `gWorkloads` | MAIN（选择命名空间） | G4（OnTaskComplete→UpdateInfoArea） | slice header 撕裂 |
| `gNamespaces` | MAIN（InitData）、G4（InitData） | MAIN（搜索、渲染列表） | 双写冲突 |
| `gFilteredNamespaces` | MAIN（InitData、搜索）、G4（InitData） | MAIN（渲染列表） | 双写冲突 |
| `gSelectedNamespace` | MAIN（InitData、选择）、G4（InitData） | MAIN、G4（UpdateInfoArea） | struct 读写竞态 |
| `gEnvironment` | MAIN（选择命名空间） | G4（UpdateInfoArea） | 指针赋值原子，字段读取非原子 |
| `gConfig` | MAIN（LoadConfig——菜单回调） | G2（time.AfterFunc） | map 并发读写 |
| `gJumpHostConfig` | MAIN（LoadConfig） | — | 低 |
| `gCloneIgnoreTagWorkload` | MAIN（LoadConfig） | MAIN（克隆操作） | 低 |

#### C 组：双重问题 —— 既有数据竞态又有 Fyne 线程违规

| 变量 | 问题 |
|------|------|
| `gInfoArea` | G4 调用 `SetText`，但 Fyne widget 方法必须在 MAIN 调用 |
| `gTaskStatusBar` | G3 调用 `Update`，非 MAIN goroutine 操作 Fyne widget |
| `gOperationButtons` | G3 调用 `Enable`/`Disable`，非 MAIN goroutine 操作 Fyne widget |

### 10.3 关键竞态场景逐一分析

#### 场景 1：gConfig map 并发读写

```
时间线：
T1: MAIN — 用户点击菜单"保存配置"
    → LoadConfig() → gConfig = newMap  ← 写 map 指针
T2: G2(AfterFunc) — 同时触发（低概率但可能）
    → GetConfig() → range gConfig["environment"].(map[interface{}]interface{})
    ← 读 map 内部结构
```

**严重程度**：低概率但后果严重。Go 的 map 在并发读写时**不是 data race 而是直接 fatal panic**（`concurrent map read and map write`）。

**当前保护**：无。

**触发条件**：用户在启动后 300ms 内恰好点击了"保存配置"菜单。窗口大时概率极低，但代码路径存在。

#### 场景 2：gSelectedWorkloads slice 竞争

```
时间线：
T1: MAIN — 用户在工作负载列表中点击多选
    → OnMultiSelected → SetSelectedWorkloads(newSlice)
    → 写 slice header (ptr, len, cap) —— 非原子！
T2: G4 — 上一个任务刚好完成
    → OnTaskComplete → UpdateInfoArea
    → 读 gSelectedWorkloads → len(gSelectedWorkloads) 检查
    → 可能读到：旧 ptr + 新 len = 越界的 slice
```

**严重程度**：中等。Go 的 slice header 是 24 字节（`Data pointer(8) + Len(8) + Cap(8)`），在 64 位系统上非原子写入。并发读可能看到：

- **旧指针 + 新长度** → 在旧内存上按新长度读取，越界访问，可能 panic
- **新指针 + 旧长度** → 只看到部分新数据，数据不一致

**验证方式**：`go run -race .` 会在 `UpdateInfoArea` 读取时直接报 race。

#### 场景 3：InitData 双写冲突

```
T1: MAIN — selectNamespace 触发 InitData（main.go:36，启动时）
    → gNamespaces = append(namespaces)
    → gFilteredNamespaces = append(...)
    → gSelectedNamespace = rancher.Namespace{} 或设置值
T2: G4 — OnTaskComplete → InitData（main.go:78）
    → gNamespaces = append(namespaces)  ← 并发写！
    → gFilteredNamespaces = append(...)
    → gSelectedNamespace = rancher.Namespace{}
```

**严重程度**：中高。两处都会触发：`TaskTypeUpdateDataComplete` 和 `TaskTypeClearData` 完成时。

用户操作"更新数据"或"清空数据"菜单后，可能在任务执行期间切换命名空间，导致：
- MAIN 和 G4 同时写 `gNamespaces` — data race
- G4 的 `gSelectedNamespace = rancher.Namespace{}` 清掉了 MAIN 刚设置的选择

**当前保护**：无。

#### 场景 4：gEnvironment nil 闪退

[info_display.go:30-31](operations/info_display.go#L30-L31) 已有 TODO 注释：

```go
// todo 修复gEnvironment偶尔为null导致闪退
info.WriteString(fmt.Sprintf("环境: %s\n", gEnvironment.Name))
```

**根因分析**：

1. G4 任务完成 → `OnTaskComplete` → `UpdateInfoArea`
2. 此时 `gSelectedNamespace.Name` 非空（刚由 InitData 恢复），但 `gEnvironment` 可能为 nil
3. 因为 `gEnvironment` 只在 `selectNamespace`（MAIN goroutine 的 UI 事件）中设置
4. InitData 恢复命名空间选择时，只恢复了 `gSelectedNamespace`，没有恢复 `gEnvironment`

这是一个逻辑 bug + 并发问题的复合体：
- **逻辑问题**：`setSelectedNamespace` 和 `setEnvironment` 是两步操作，中间状态不一致
- **并发问题**：G4 可能在两步之间读取到不一致状态

#### 场景 5：Fyne Widget 跨 goroutine 访问

```
G3（UI 定时器）每 500ms:
  → taskStatusBar.Update(status, dots, taskInfo, current, total)
  → btn.Enable() / btn.Disable()

G4（任务执行器）任务完成时:
  → gInfoArea.SetText(info.String())
```

**Fyne 官方要求**：所有 widget 方法必须在创建 window 的 goroutine（即 MAIN）中调用。

**当前行为**：G3 和 G4 直接调用 widget 方法。

**可能后果**：
- 界面闪烁、更新不及时
- 某些平台（尤其 macOS）上可能导致崩溃
- Fyne 内部状态机使用 `sync.Mutex` 保护部分字段，但渲染管线假设单线程访问
- 目前"能工作"但不保证未来版本继续工作

### 10.4 完整调用链追踪

下面追踪一条"用户点击更新数据 → 任务完成 → UI 刷新"的完整路径，标注每一步所在的 goroutine：

```
[MAIN] 用户点击菜单"更新数据"
  → main_window.go:45 UpdateDataTask(env, db, taskQueue)
    → workload_ops.go:194 taskQueue.AddTask(UpdateData)
    → workload_ops.go:207 taskQueue.AddTask(UpdatePortMap)
    → workload_ops.go:208 taskQueue.AddTask(Delay 500ms)
    → workload_ops.go:209 taskQueue.AddTask(UpdatePod)
  → main_window.go:54 UpdateDataCompleteTask
    → taskQueue.AddTask(UpdateDataComplete)

[G4] 消费 channel，开始执行任务:
  → executeTask: UpdateEnvironment → 读 API、写 DB        ← 不碰全局变量
  → executeTask: UpdateService → 读 API、写 DB             ← 不碰全局变量
  → executeTask: Delay 500ms                               ← 不碰全局变量
  → executeTask: UpdatePod → 读 API、写 DB                 ← 不碰全局变量
  → executeTask: UpdateDataComplete → result.Success=true
  → OnTaskComplete(task, result)
    ↓
[G4] main.go:78 → operations.InitData()
    → 写 gNamespaces              ← RACE with MAIN reading
    → 写 gFilteredNamespaces      ← RACE with MAIN reading
    → 写 gSelectedNamespace       ← RACE with MAIN reading/writing
    → 读 gNamespaceList → Select/Refresh  ← Fyne widget from G4!
[G4] main.go:80 → operations.UpdateInfoArea()
    → 读 gSelectedWorkloads       ← RACE with MAIN writing
    → 读 gSelectedNamespace       ← RACE
    → 读 gEnvironment             ← RACE, 可能 nil
    → gInfoArea.SetText(...)      ← Fyne widget from G4!
```

### 10.5 修复方案

#### 方案 A：最小侵入修复（推荐先行）

**思路**：不改架构，只加锁 + 修复回调路径。

**改动范围**：约 3 个文件，< 50 行代码。

```
1. operations/globals.go:
   - 新增 sync.RWMutex，所有 Getter/Setter 加读锁/写锁
   - LoadConfig、InitData 加写锁

2. main.go (taskQueueUI):
   - UpdateStatus 加读锁
   - OnTaskComplete 不直接调 InitData/UpdateInfoArea
   - 改为设置 dirty flag

3. taskqueue.go (UI ticker):
   - 每 500ms 检查 dirty flag
   - 发现 dirty 后调 InitData/UpdateInfoArea（仍在 G3，需配合 Fyne 方案 B）
```

**优点**：
- 改动小，风险低
- 彻底消除数据竞态（data race）
- 不改变现有调用方式

**缺点**：
- 没有解决 Fyne widget 跨 goroutine 访问问题
- `gInfoArea` 等 UI 组件仍在业务层
- 锁粒度粗，InitData 持锁期间可能阻塞 UI 渲染

**引入风险**：
- 如果某个 Getter 调用后、使用返回值前释放了锁，而期间另一个 goroutine 修改了数据，返回值已过期 —— 这本来就有，不算新问题
- 死锁：如果 Getter 内持读锁时回调了某个会调 Setter 的函数 → 写锁请求等读锁释放 → 读锁等回调返回 → **死锁**。需要审查所有 Getter → 回调路径。

#### 方案 B：中等改动（推荐最终采用）

**思路**：抽取 AppState + UI 更新通道。

```
新增文件: operations/state.go

type AppState struct {
    mu sync.RWMutex
    
    // 业务数据
    config                 map[string]interface{}
    environment            *rancher.Environment
    jumpHostConfig         *rancher.JumpHostConfig
    cloneIgnoreTagWorkload []string
    
    namespaces         []rancher.Namespace
    filteredNamespaces []rancher.Namespace
    selectedNamespace  rancher.Namespace
    
    workloads         []rancher.Workload
    filteredWorkloads []rancher.Workload
    selectedWorkloads []rancher.Workload
    
    // 数据库和任务队列（启动时注入，之后只读）
    db        *rancher.DatabaseManager
    taskQueue *rancher.TaskQueue
    
    // Fyne widgets（启动时注入，之后只读，但方法调用需在主线程）
    namespaceList   *widget.List
    namespaceSearch *widget.Entry
    workloadList    *component.MultiSelectList
    workloadSearch  *widget.Entry
    infoArea        *widget.Entry
    taskStatusBar   *component.TaskStatusBar
    operationButtons []*widget.Button
    
    // 跨 goroutine 通信
    needInitData    bool          // G4 设置，UI ticker 检查
    needUpdateInfo  bool          // 同上
    uiUpdateCh      chan func()   // 待处理 UI 更新
}
```

**解决 Fyne 线程问题的方法**：

由于 Fyne 没有 `RunOnMainThread` API，可以仿照实现一个简易版：

```go
// main.go — 在 ShowAndRun 之前启动
uiUpdateCh := make(chan func(), 100)

// 启动一个 goroutine 读 channel，用 time.Ticker 在"近似主线程"执行
// 实际上会触发 Fyne 内部的定时器来驱动
go func() {
    for fn := range uiUpdateCh {
        fn() // 注意：这里仍然不是真正的主线程
    }
}()
```

由于 Go 中 Fyne 的 GLFW 主线程是 `runtime.LockOSThread` 锁定的，任何其他 goroutine 都无法冒充。**对于 Fyne 桌面应用，要完全解决跨线程 widget 访问，唯一正确途径是让所有 UI 更新从 Fyne 事件回调中触发。**

实际操作建议：

1. **InitData/UpdateInfoArea**：G4 完成时不直接调用，改设 flag；在 MAIN 的下一个事件循环中批量处理
2. **利用 Fyne 的 `Canvas.Refresh()`**：在 G4 中调用 `window.Canvas().Refresh()` 会触发重绘，可以在自定义 widget 的 render 中检查 flag —— 但这会耦合渲染和业务
3. **实用妥协**：Fyne 内部许多 widget 方法用了 mutex 保护，`SetText`/`Enable`/`Disable` 等简单操作从非主线程调用在 Linux/Windows 上大概率安全（macOS 风险较高）。可以在代码中加注释标记这些调用点，等未来 Fyne 提供官方 API 后迁移。

#### 方案对比

| 维度 | 方案 A（加锁） | 方案 B（AppState + 通道） |
|------|---------------|--------------------------|
| 代码改动量 | ~50行 | ~300行 |
| 消除 data race | ✅ 是 | ✅ 是 |
| 解决 Fyne 线程问题 | ❌ 否 | ⚠️ 部分（受 Fyne API 限制） |
| 可测试性 | ⚠️ 改善（可 mock 状态） | ✅ 大幅改善 |
| 回归风险 | 低（死锁需审查） | 中（重构范围大） |
| 后续可维护性 | 一般 | 好 |

#### 建议实施步骤

```
第1步（立即）: 方案 A
  - 加 RWMutex 消除所有 data race
  - OnTaskComplete 不直接调 InitData，设 dirty flag
  - 审查所有 Getter→Setter 路径确保无死锁
  - 用 go test -race 验证

第2步（短期）: gEnvironment nil 修复
  - setSelectedNamespace 和 setEnvironment 合并为原子操作
  - UpdateInfoArea 加 nil 检查（紧急修复）

第3步（中期）: 方案 B
  - 逐步迁移到 AppState
  - 选代而非一次性重写
  - 每个迁移一个变量组，确保测试通过

第4步（长期）: 
  - 等待 Fyne 官方提供 RunOnMainThread API
  - 彻底解决 widget 跨 goroutine 访问
```
