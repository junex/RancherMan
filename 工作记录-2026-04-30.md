# 工作记录 2026-04-30

## 问题背景

代码审查报告 Issue 1.2「分层架构破裂」修复后（commit `19d6cfa`），出现列表显示异常和点击崩溃问题。

## 修复内容

### 修复1：GetEnvironment() nil 指针崩溃 (P0)

**文件**: `operations/info_display.go`

- `updateInfoAreaForSelectNamespace`: `GetEnvironment().Name` 处加 nil 检查
- `updateInfoAreaForSingleWorkload`: `GetEnvironment().Ip` 处加 nil 检查

**影响**: 当 Environment 未正确设置时（如 `GetEnvironmentFromConfig` 失败），不再闪退。

### 修复2：InitData 时序调整

**文件**: `main.go`

`InitData()` 从 `ShowAndRun()` 之前移到之后（30ms 延迟），确保 Fyne 渲染管线就绪后才刷新列表。

### 修复3：InitData UI 操作统一走 dispatcher

**文件**: `operations/globals.go`

`selectIndex < 0` 路径之前直接调用 `nsList.UnselectAll()`/`ScrollToTop()`/`Refresh()`，未通过 `fyne.Do`。改为与 `selectIndex >= 0` 路径一致，全部通过 `dispatcher.RunOnUI()` 调度。

## 待解决：数据库 schema 不匹配

**现象**: `GetAllNamespacesDetail()` 查询 namespace 表时报错：
```
sql: Scan error on column index 0, name "id": converting driver.Value type string ("big-data") to a uint
```

**排查过程**:
- 新增诊断代码打印 namespace 表 PRAGMA table_info，确认实际 schema 与 Go struct 不匹配
- Git 历史确认 `Namespace` struct（`ID uint, Name string...`）自 `864f086` 引入后从未变过
- 问题可能是 SQLite 数据库文件本身被其他版本代码写入导致 schema 不一致

**临时方案**: 删库重建（保留 config 配置）
```powershell
# 先通过"显示配置"菜单复制 YAML 内容
# 然后：
del "$env:APPDATA\Rancher助手\app.db"
# 重启应用，粘贴配置，运行"更新数据"
```
