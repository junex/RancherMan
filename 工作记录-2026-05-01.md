# 工作记录 2026-05-01

## 问题背景

`fix/global-state-concurrency` 分支（修复1.1 + 1.2）基于旧的 dev（commit `4fb6189`）创建，缺少 origin/dev 上的 6 个提交。需合并回 dev。

## 分支状态

```
fix/global-state-concurrency (3 commits):
  d46798a fix: GetEnvironment nil崩溃 + InitData时序/线程安全修复
  19d6cfa fix: operations层去除Fyne依赖，通过接口解耦UI层
  10ddd96 fix: 全局变量并发安全改造 — AppState + RWMutex + fyne.Do

origin/dev 缺失的 6 个 commit:
  78332b0 feat 数据库启动延时3秒
  b55228d fix 修复无数据时的闪退
  67314f1 fix workload列表上下跳动
  bea49e7 feat workload背景颜色变化
  e59f3e0 feat websocket接受pod变化
  2807f4f refactor 改一下api类的关系
```

两者共同基线: `4fb6189 feat 显示进度时，跳过延时任务`

## 操作: dev 分支 merge fix/global-state-concurrency

### 冲突文件及解决策略

| 文件 | 策略 |
|------|------|
| go.mod | 保留两边 — fyne v2.7.1 + crypto v0.33.0 + gorilla/websocket + yaml.v2 |
| go.sum | 取 fix 分支版本 + go mod tidy 重新生成 |
| main.go | fyne.DoAndWait 包裹 + 保留 TaskTypeUpdatePod WebSocket + 保留 ui.UpdateInfoArea() |
| operations/globals.go | 采用 AppState 线程安全模式（fix分支） |
| operations/info_display.go | getter 模式 + nil安全检查 + 保留完整多工作负载显示 |
| ui/globals.go | 保留 gInfoAreaStatus/InfoAreaStatus，删除已废弃的 gTaskStatusBar/gOperationButtons |

### 合并后修复的问题

1. **GPodStateMapAfterUpdateInfoArea 未填充** — `updateInfoAreaForSelectNamespace` 只取 podList 但不写全局 map，导致工作负载列表 pod 状态颜色标识失效。补上了赋值。

2. **死代码清理** — `gTaskStatusBar` 和 `gOperationButtons` 在 ui/globals.go 只有声明无引用（fix 分支已改用 InitResult 传递），删除。

## 验证

- go build ./... 编译通过
- go vet 仅报告 1 个已有问题（ResourceWebSocket 锁拷贝，非本次引入）
- 修复 1.1（AppState + RWMutex + fyne.Do）完整保留
- 修复 1.2（operations 层去除 Fyne 依赖）完整保留
- operations 包零 Fyne import
- InitResult → taskQueueUI 链路正确
